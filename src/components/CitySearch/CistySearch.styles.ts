import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	container: {
		marginBottom: 15
	},
	input: {
		fontSize: 20,
		margin: 10,
		padding: 10,
		width: "95%",
		borderWidth: 1
	}
});
